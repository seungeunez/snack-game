# 프로젝트 : 과자 먹기 게임 (Snack Catcher)

## 목표
- 브라우저에서 실행되는 아케이드 스타일 이동 게임
- 사람 캐릭터를 조작해 떨어지는(또는 등장하는) 아이템을 먹으며 점수를 획득
- 과자는 점수를 올리고, 똥·벌레는 점수를 깎아 위험 요소로 만듦

## 기능

### 핵심 플레이
- 플레이 영역 안에서 **사람 모양 캐릭터**를 키보드로 이동
- **과자**와 충돌(먹기) 시 점수 증가
- **똥**, **벌레**와 충돌 시 점수 감소
- 화면 밖으로 나가지 않도록 경계 처리
- 언어는 무조건 한국어

### 조작
- 방향키 또는 `W` `A` `S` `D`로 이동
- `Space` 또는 버튼으로 게임 시작 / 재시작

### 아이템
| 종류 | 효과 | 기본 점수 (예시) |
|------|------|------------------|
| 과자 | 점수 증가 | +10 |
| 똥   | 점수 감소 | -15 |
| 벌레 | 점수 감소 | -10 |

- 아이템은 일정 간격으로 생성되며 위에서 아래로 떨어짐
- 생성 비율은 과자가 많고, 똥·벌레는 적게 나오도록 가중치 설정
- 먹은 아이템은 즉시 제거

### 점수 · 게임 흐름
- 현재 점수, 최고 점수를 화면에 표시
- 최고 점수는 `localStorage`에 저장
- 제한 시간(예: 60초) 종료 시 게임 오버
- 점수가 0 미만이 되면 0으로 고정 (마이너스 무한 방지)
- 게임 오버 시 결과 메시지와 재시작 버튼 표시

### 피드백
- 과자 획득 / 감점 시 짧은 메시지 또는 색상 피드백
- 감점 아이템 섭취 시 캐릭터 또는 화면에 짧은 흔들림·색 변화 등 시각 피드백

## 기술
- HTML, CSS, JavaScript (ES Module)
- 게임 필드는 `<canvas>` 사용
- 점수·버튼·안내 문구는 HTML UI로 분리
- `requestAnimationFrame` 기반 게임 루프
- 충돌 판정: 사각형(AABB) 기준
- 별도 프레임워크 없이 바닐라 JS만 사용

## 대상
- **중급 개발자**
- 모듈 분리, 게임 루프, 상태 관리, 충돌 처리를 직접 구현해 볼 수 있는 수준
- 파일 역할이 분리되어 있어 로직을 찾고 수정하기 쉬운 구조

## 폴더 구조
```text
snack/
├─ index.html
├─ css/
│  └─ style.css
├─ js/
│  ├─ main.js        # 진입점, 이벤트 연결, 루프 시작
│  ├─ game.js        # 게임 루프, 업데이트·렌더 오케스트레이션
│  ├─ player.js      # 플레이어 이동·그리기
│  ├─ items.js       # 아이템 생성·이동·제거
│  ├─ collision.js   # 충돌 판정
│  ├─ ui.js          # 점수·메시지·게임 오버 UI
│  ├─ state.js       # 게임 상태 (점수, 시간, 플래그)
│  └─ config.js      # 점수, 속도, 스폰 간격 등 상수
├─ images/
│  ├─ player.svg
│  ├─ snack.svg
│  ├─ poop.svg
│  └─ bug.svg
├─ PRD.md
└─ server.js
```

## 파일 역할 설명

- `index.html`
  - 캔버스, 점수 패널, 시작/재시작 버튼, 안내 문구 구조 정의

- `css/style.css`
  - 레이아웃, 캔버스 테두리, HUD(점수·타이머), 버튼·다이얼로그 스타일

- `js/main.js`
  - 앱 초기화, 키보드 입력 리스너 등록, `game.start()` 호출

- `js/game.js`
  - `requestAnimationFrame` 루프
  - 매 프레임: 입력 반영 → 아이템 업데이트 → 충돌 검사 → 그리기 → UI 갱신
  - 게임 시작·일시정지·종료 흐름 관리

- `js/player.js`
  - 플레이어 위치·속도·크기
  - 키 입력에 따른 이동 및 경계 클램핑
  - 캔버스에 사람 스프라이트(또는 도형) 렌더

- `js/items.js`
  - 아이템 타입(`snack`, `poop`, `bug`)별 데이터
  - 스폰 타이머, 낙하, 화면 밖 제거
  - 타입별 점수는 `config.js` 참조

- `js/collision.js`
  - 플레이어와 아이템 AABB 충돌 여부 판별
  - 충돌 시 콜백으로 점수 처리·아이템 제거 트리거

- `js/ui.js`
  - 점수, 남은 시간, 상태 메시지 DOM 갱신
  - 게임 오버 패널 표시/숨김

- `js/state.js`
  - `score`, `bestScore`, `timeLeft`, `isPlaying`, `isGameOver` 등 보관
  - 점수 증감, 최고 점수 저장, 리셋 함수 제공

- `js/config.js`
  - 캔버스 크기, 플레이어·아이템 속도
  - 타입별 점수, 스폰 간격, 게임 시간, 스폰 가중치

- `images/`
  - 사람, 과자, 똥, 벌레 SVG(또는 PNG) — 없으면 캔버스 도형으로 대체 가능

- `server.js`
  - 로컬에서 정적 파일 서빙용 (ES Module CORS 대응)

## 게임 규칙 요약
1. 게임 시작 후 제한 시간 동안 플레이
2. 과자를 먹으면 점수 상승
3. 똥·벌레를 먹으면 점수 하락
4. 시간 종료 시 최종 점수 표시, 최고 점수 갱신 시 저장
5. 재시작 시 점수·아이템·타이머 초기화

## 구조 설계 의도
- **중급** 수준에 맞게 `game / player / items / collision`로 책임 분리
- UI와 게임 로직을 분리해 캔버스 렌더와 DOM HUD를 동시에 다루는 패턴 학습
- `config.js`에 밸런스 수치를 모아 점수·난이도 조정이 쉽게 구성
- `puzzle-game`보다 모듈은 많지만, 각 파일은 단일 역할만 담당해 유지보수 용이
- 이미지 없이도 도형으로 동작 가능하도록, 그리기 로직은 스프라이트 교체가 쉬운 형태로 작성

## 향후 확장 (선택)
- 난이도에 따라 낙하 속도·스폰 비율 증가
- 연속 과자 획득 시 콤보 보너스
- 무적 시간, 슬로우 모션 등 파워업 아이템
